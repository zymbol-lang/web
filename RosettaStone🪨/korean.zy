// FizzBuzz — 한국어
// 식별자는 한국어로. 연산자는 항상 기호.

>> "안녕하세요, 한국어 세계!" ¶

분류(숫자) {
    ? 숫자 % 15 == 0 { <~ "쉬익붕붕" }
    _? 숫자 % 3  == 0 { <~ "쉬익" }
    _? 숫자 % 5  == 0 { <~ "붕붕" }
    _ { <~ 숫자 }
}

@ 이:1..15 {
    >> 분류(이) ¶
}
