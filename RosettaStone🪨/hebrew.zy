// FizzBuzz — עברית
// מזהים בעברית. אופרטורים תמיד סמליים.

>> "שלום, עולם דובר עברית!" ¶

סיווג(מספר) {
    ? מספר % 15 == 0 { <~ "בועהזמזום" }
    _? מספר % 3  == 0 { <~ "בועה" }
    _? מספר % 5  == 0 { <~ "זמזום" }
    _ { <~ מספר }
}

@ נ:1..15 {
    >> סיווג(נ) ¶
}
