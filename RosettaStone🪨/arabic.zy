// FizzBuzz — العربية
// المعرّفات بالعربية. المشغّلات دائماً رمزية.

>> "مرحباً، عالم ناطق بالعربية!" ¶

تصنيف(عدد) {
    ? عدد % 15 == 0 { <~ "فقاعةطنين" }
    _? عدد % 3  == 0 { <~ "فقاعة" }
    _? عدد % 5  == 0 { <~ "طنين" }
    _ { <~ عدد }
}

@ ن:1..15 {
    >> تصنيف(ن) ¶
}
