// @skip-parity: بوم به یک پایانهٔ واقعی زیر خط فرمان و به یک لایهٔ بوم در مرورگر
// @skip-parity: نیاز دارد، پس اجرا از راه لوله چیزی برای مقایسه باقی نمی‌گذارد.
// ============================================================
// mandelbrot — مجموعهٔ ماندلبرو، کشیده بر بوم پایانه.
// نام‌ها به فارسی. نشانه‌ها همیشه نشانه می‌مانند.
//
// یک عدد همه چیز را تعیین می‌کند: ‏`تاب`‏، مدار پیش از گریختن چند گام
// تاب می‌آورد. همان است که حرف را برمی‌گزیند و همان است که رنگ را
// برمی‌گزیند؛ پس تصویر نمی‌تواند با خودش ناسازگار باشد. ‏`تاب = ۰`‏
// یعنی هرگز نگریخت — آن خودِ مجموعه است، پُر کشیده.
//
//   >>|  بوم را می‌گشاید   >>!  پاک می‌کند   >>?  اندازه می‌گیرد
//   >>~  یک خانه می‌نویسد، در یک سطر، یک ستون، با یک رنگ
//   <<|  منتظر یک کلید می‌ماند
//
// دو صرفه‌جویی، و هر دو در تصویر دیده می‌شوند نه در اندازه‌گیری:
// مربع‌ها از گامی به گام بعد حمل می‌شوند به جای آنکه دوباره حساب
// شوند، و مجموعه گرد محور حقیقی آینه می‌شود، پس تنها نیمهٔ بالا
// می‌گریزد. این دو با هم بهای وضوحی را می‌پردازند که جزئیات می‌خواهد.
// ============================================================

// بیرون از مجموعه: از کم‌رنگ تا پُر، از سبز تیره تا روشن.
شیب    = " .:-=+*#%"
رنگ‌ها = [۲۲, ۲۸, ۳۴, ۴۰, ۴۶, ۸۲, ۱۱۸, ۱۵۴, ۱۹۱]

// گام‌هایی که مدار پیش از گریختن تاب آورد؛ اگر هرگز نگریخت، صفر.
// مدار_حقیقی۲ و مدار_موهومی۲ با آن حمل می‌شوند، پس هر گام چهار ضرب هزینه دارد نه شش.
گریز(نقطه_حقیقی, نقطه_موهومی) {
    مدار_حقیقی   = ۰.۰
    مدار_موهومی  = ۰.۰
    مدار_حقیقی۲  = ۰.۰
    مدار_موهومی۲ = ۰.۰
    @ دور:۱..۳۶ {
        ? مدار_حقیقی۲ + مدار_موهومی۲ > ۴.۰ { <~ دور }
        مدار_موهومی  = ۲.۰ * مدار_حقیقی * مدار_موهومی + نقطه_موهومی
        مدار_حقیقی   = مدار_حقیقی۲ - مدار_موهومی۲ + نقطه_حقیقی
        مدار_حقیقی۲  = مدار_حقیقی * مدار_حقیقی
        مدار_موهومی۲ = مدار_موهومی * مدار_موهومی
    }
    <~ ۰
}

>>| {
    >>!
    (بلندا, پهنا) = >>?
    سطرها   = بلندا - ۲
    ستون‌ها = پهنا  - ۱

    // یک خانه نزدیک ۲٫۵ برابر پهنایش بلند است؛ گسترهٔ عمودی با همان
    // مقیاس می‌شود تا مجموعه گرد درآید.
    پهنای_صفحه  = ۲.۷
    بلندای_صفحه = سطرها * پهنای_صفحه * ۲.۵ / ستون‌ها
    میانه       = سطرها / ۲ + ۱

    @ سطر:۱..میانه {
        آینه = سطرها + ۱ - سطر
        @ ستون:۱..ستون‌ها {
            نقطه_حقیقی  = (ستون - ۱) * پهنای_صفحه  / ستون‌ها - ۲.۱
            نقطه_موهومی = (سطر  - ۱) * بلندای_صفحه / سطرها   - بلندای_صفحه / ۲.۰
            تاب         = گریز(نقطه_حقیقی, نقطه_موهومی)
            نشانه       = "@"
            رنگ         = ۲۳۱
            ? تاب <> ۰ {
                گام = تاب / ۴ + ۱
                ? گام > ۹ { گام = ۹ }
                نشانه = شیب[گام]
                رنگ   = رنگ‌ها[گام]
            }
            >>~ (سطر, ستون, ۰, رنگ) > نشانه
            ? آینه > میانه { >>~ (آینه, ستون, ۰, رنگ) > نشانه }
        }
    }

    >>~ (بلندا, ۲, ۰, ۲۴۴) > "برای خروج هر کلیدی را بزنید"
    <<| _کلید
}
