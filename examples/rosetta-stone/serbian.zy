// FizzBuzz — Српски
// Идентификатори на српском. Оператори су увек симболични.

>> "Здраво, Српски свете!" ¶

класификуј(број) {
    ? број % 15 == 0 { <~ "ШишЗуј" }
    _? број % 3  == 0 { <~ "Шиш" }
    _? број % 5  == 0 { <~ "Зуј" }
    _ { <~ број }
}

@ и:1..15 {
    >> класификуј(и) ¶
}
